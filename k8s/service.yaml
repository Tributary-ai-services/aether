apiVersion: v1
kind: Service
metadata:
  name: aether-frontend
  labels:
    app: aether-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: aether-frontend